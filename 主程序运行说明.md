
## 后台检测程序 (main.exe) 操作与维护手册

### 1. 概述

本视觉检测系统的主程序 `main.exe` 是一个**后台服务程序**。这意味着当它正常运行时，您**不会看到任何窗口或用户界面**。它会在后台默默地执行相机图像采集、缺陷分析和数据上报等任务。

由于没有可见的窗口，所有对该程序的操作（尤其是关闭操作）都需要通过 **Windows 任务管理器** 来完成。

### 2. 如何安全地停止 (关闭) 程序

在进行任何维护操作（如重启设备）或程序更新之前，必须先完全停止 `main.exe`。请严格按照以下步骤操作：

**步骤 1：打开任务管理器**

您可以通过以下任何一种方式打开任务管理器：
*   **快捷键**：同时按下 `Ctrl` + `Shift` + `Esc` 键。
*   **任务栏**：在屏幕底部的任务栏空白处点击鼠标右键，然后选择“任务管理器”。
*   **命令**：按下 `Win` + `R` 键，输入 `taskmgr`，然后按回车。

**步骤 2：找到 `main.exe` 进程**

1.  在任务管理器窗口中，点击**“详细信息”**选项卡。这个视图能最清晰地显示所有正在运行的程序。
2.  在“名称”列下，找到名为 **`main.exe`** 的进程。您可以点击“名称”列的标题进行排序，以便更容易找到它。


*(这是一个示意图，您看到的界面可能略有不同)*
![alt text](image.png)

**步骤 3：结束进程**

1.  用鼠标**右键**点击 `main.exe` 进程。
2.  在弹出的菜单中，选择**“结束任务”**或**“结束进程”**。
3.  系统可能会弹出一个确认窗口，询问您是否确定要结束该进程。请点击**“结束进程”**按钮进行确认。

**步骤 4：确认程序已关闭**

操作完成后，`main.exe` 应该会从“详细信息”列表中消失。这表示程序已成功关闭。

### 3. 重要注意事项与操作场景

#### **黄金法则：重启程序 = 先彻底停止，再重新运行**

无论在何种情况下需要重启程序，都**严禁**在旧进程未关闭的情况下直接双击运行新的 `main.exe`。这会导致端口和硬件资源冲突，使系统无法正常工作。

---

#### **情况一：整机或相机断电后恢复**

如果检测设备或任何一台相机意外断电，恢复供电后，必须重启检测程序以确保软件和硬件状态同步。

**处理流程：**
1.  **确认硬件已上电稳定**：确保所有相机、剔废控制器和计算机的电源都已恢复正常。等待约半分钟，让相机完成自身的启动过程。
2.  **关闭软件**：按照上一章节的步骤，使用任务管理器**彻底关闭**正在运行的 `main.exe` 进程。
3.  **等待片刻**：关闭进程后，等待5-10秒，以确保所有资源都已释放。
4.  **重新运行**：双击桌面或指定路径的 `main.exe` 图标，重新启动检测程序。程序会自动重新连接相机并恢复工作。

---

#### **情况二：程序无响应或出现异常**

如果您发现系统停止检测、实时画面卡住不动，或者出现其他异常行为，最直接的解决方法就是重启程序。

**处理流程：**
1.  **关闭软件**：使用任务管理器**彻底关闭** `main.exe` 进程。
2.  **重新运行**：等待5-10秒后，重新启动 `main.exe`。

---

#### **情况三：修改了配置文件 `config.json`**

如果您对 `config.json` 文件进行了任何修改（例如调整算法参数、剔废延迟等），这些改动**不会立即生效**。您必须重启程序来加载新的配置。

**处理流程：**
1.  **保存配置文件**：确保您对 `config.json` 的修改已保存。
2.  **关闭软件**：使用任务管理器**彻底关闭** `main.exe` 进程。
3.  **重新运行**：重新启动 `main.exe`，程序将以新的配置参数运行。
