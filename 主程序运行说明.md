

## 后台检测程序 (`main.exe`) 操作与维护手册

### **1. 程序简介**

本视觉检测系统的主程序 `main.exe` 是一个**后台服务程序**。

这意味着当它正常启动并运行时，您**不会看到任何传统的程序窗口或用户界面**。它会在计算机后台安静地执行所有核心任务，包括：

*   连接并控制所有相机。
*   实时采集生产线上的产品图像。
*   对图像进行分析以检测缺陷。
*   将检测结果和状态数据发送到中央服务器。

您可以通过访问指定的监控网址来查看系统的实时检测画面和运行状态。

#### **如何确认程序是否正在运行？**

确认程序是否在后台运行的唯一方法是通过 **Windows 任务管理器**。

1.  通过快捷键 `Ctrl` + `Shift` + `Esc` 打开任务管理器。
2.  在任务管理器窗口中，点击**“详细信息”**选项卡。
3.  在“名称”列下，查找名为 **`main.exe`** 的进程。

如果列表中存在 `main.exe`，则表明程序正在正常运行。

<br>

![alt text](image-1.png)

<br>

---

### **2. 如何安全地停止 (关闭) 程序**

在进行任何设备维护、系统重启或软件更新之前，必须先将后台的 `main.exe` 程序完全停止。

请严格按照以下步骤操作：

**步骤 1：打开任务管理器**

您可以通过以下任一方式打开任务管理器：
*   **快捷键**：同时按下 `Ctrl` + `Shift` + `Esc` 键。
*   **任务栏**：在屏幕底部的任务栏空白处点击鼠标右键，然后选择“任务管理器”。

<br>

![alt text](image.png)

<br>

**步骤 2：找到并结束进程**

1.  在任务管理器窗口中，确保您位于**“详细信息”**选项卡下。
2.  在进程列表中找到名为 **`main.exe`** 的条目。
3.  用鼠标**右键**点击 `main.exe` 进程。
4.  在弹出的菜单中，选择**“结束任务”**。

<br>

![alt text](image-2.png)

<br>

5.  系统可能会弹出一个确认窗口，询问您是否确定要结束该进程。请点击**“结束进程”**按钮进行最终确认。

**步骤 3：确认程序已关闭**

操作完成后，`main.exe` 进程会从“详细信息”列表中消失。这表示程序已成功并被安全地关闭。

---

### **3. 重要操作场景与注意事项**

#### **核心原则：重启 = 先彻底停止，再重新运行**

在任何情况下需要重启程序，都**严禁**在旧进程未关闭的情况下直接双击运行新的 `main.exe`。这会导致系统资源冲突，使程序无法正常工作。

#### **场景一：设备或相机断电后恢复**

如果检测设备、任何一台相机或计算机主机意外断电，当供电恢复后，必须重启检测程序以确保软硬件状态的同步。

*   **处理流程：**
    1.  **确认硬件稳定**：确保所有相机和计算机的电源已恢复正常，等待约一分钟，让硬件设备完成自启动。
    2.  **关闭软件**：按照第2节的步骤，使用任务管理器**彻底关闭**可能仍在运行的 `main.exe` 进程（即使您认为它已经因断电而关闭）。
    3.  **重新运行**：等待10秒钟，然后双击 `main.exe` 的图标，重新启动检测程序。

#### **场景二：系统无响应或画面卡住**

如果您通过监控网址发现系统停止检测、实时画面卡住不再更新，或者出现其他异常行为，最直接有效的解决方法就是重启程序。

*   **处理流程：**
    1.  **关闭软件**：使用任务管理器**彻底关闭** `main.exe` 进程。
    2.  **重新运行**：等待10秒钟后，重新启动 `main.exe`。

#### **场景三：修改了系统配置文件 (`config.json`)**

如果您根据我方技术支持人员的指导，对 `config.json` 文件进行了任何修改（例如调整剔废延迟、缺陷尺寸阈值等），这些改动**不会立即生效**。您必须重启程序来加载新的配置。

*   **处理流程：**
    1.  **保存配置文件**：确保您对 `config.json` 的修改已完全保存。
    2.  **关闭软件**：使用任务管理器**彻底关闭** `main.exe` 进程。
    3.  **重新运行**：重新启动 `main.exe`，程序将会以您新修改的配置参数来运行。

---

### **4. 快速总结**

*   `main.exe` 是一个**没有窗口的后台程序**。
*   停止或关闭程序的**唯一且正确**的方法是使用**任务管理器**来“结束任务”。
*   在任何硬件断电重启、软件无响应或修改配置后，都必须**先停止旧的 `main.exe` 进程，然后再重新启动它**。