# 系统使用与验收手册

## 1. 系统概览

本系统用于在线玻璃缺陷检测，核心组成：
- 后台检测程序：`main.exe`
- 配置文件：`config.yaml`（优先）/ `config.json`
- 多相机采集与网络绑定：通过 MAC 地址与逻辑索引稳定映射
- Web 服务：用于状态查看、结果上报

## 2. 程序运行与关闭

`main.exe` 为“无界面程序”，默认在前台短暂显示控制台后进入运行。待系统开机稳定运行3分钟后，双击桌面程序"main 快捷方式"即可启动。

![alt text](image-2.png)

当前版本不属于严格意义的系统服务，支持以下两种关闭方式：

### 2.1 方式一：键盘 Ctrl + C（推荐，优雅退出）
- 在控制台光标处于 `main.exe` 窗口内时，按 `Ctrl + C`；
- 程序收到信号后会依次停止采集、保存必要数据并关闭硬件连接；
- 适合常规维护、修改配置后重启等场景。

占位（示意图：在控制台中按 Ctrl + C）

![插图占位：Ctrl+C 关闭示意](image.png)

### 2.2 方式二：任务管理器结束进程（应急，强制退出）
- 使用 `Ctrl + Shift + Esc` 打开任务管理器；
- 切换到“详细信息”，右键 `main.exe`，选择“结束任务”；
- 适用于程序无响应或无法切换到控制台的应急场景；
- 强制结束可能中断未完成的上传/保存，建议优先使用 Ctrl + C。

占位（示意图：任务管理器结束 main.exe）

![插图占位：任务管理器结束进程](image-1.png)

> 重启原则：任何需要重启的情况，务必先完全关闭旧进程，再重新启动 `main.exe`。如果遇到相机断电或者设备断电的情况，推荐重启电脑并按照操作流程重新运行程序。

## 3. 配置参数（config.yaml / config.json）

仅修改本节中说明的基础参数；高级算法参数由技术支持配置。

### 3.1 通用设置

| 参数 | 说明 |
| :--- | :--- |
| `storage_path` | 保存检测图片与报告的目录 |
| `lineName` | 产线名称（用于后台数据区分） |

### 3.2 服务器通信

| 参数 | 说明 |
| :--- | :--- |
| `server` | 后台服务器地址/IP |
| `upload_url` | 缺陷报告上传地址（通常替换 IP 即可） |
| `stats_push_url` | 产量/剔废数据上报地址 |
| `heartbeat_url` | 系统心跳上报地址 |
| `listen_port` | 本地控制服务端口（一般无需修改） |

### 3.3 剔废控制

| 参数 | 说明 |
| :--- | :--- |
| `max_defect_size_mm` | 自动剔废尺寸阈值（≥ 即触发） |
| `REJECTION_DELAY_S` | 剔废延迟（拍照点到剔废点运行时间） |
| `REJECTION_PULSE_MS` | 剔废脉冲时长（硬件相关，通常不改） |

> 其余 `system_params`、`camera_setup`、`defect_detection_params` 等属于高级参数，勿随意修改。

## 4. 相机索引与网络绑定

### 4.1 两类索引
- 物理索引（Physical Index）：相机 SDK 运行时分配，每次启动可能变化；
- 逻辑索引（Logical Index）：在配置中定义，通过 MAC 绑定，编号稳定（cam_index）。

### 4.2 网络与 IP 分配原则
- 以相机 MAC 为唯一标识进行网卡绑定；
- 已配置相机保持原 IP（在同网段时）；
- 新相机基于 MAC 尾字节计算 IP；
- 绑定关系持久化在配置文件中，逻辑索引稳定不受启动顺序影响。

## 5. 算法流程简介（采集 → 处理 → 发送）

为便于理解，以下为高层流程概述（不涉及实现细节）：

1) 采集数据
- 各相机按设定参数（分辨率、曝光、触发方式）持续获取图像；
- 每帧按 ROI 模板提取需要处理的区域，进入处理队列。

2) 处理数据
- 预处理与增强：去噪、对比度/亮度增强等；
- 结构提取：边缘/多边形轮廓、角点/角度、连通域特征提取；
- 缺陷判定：依据尺寸、形态与角度偏差等规则计算，给出 OK/NG 与缺陷类别；
- 结果渲染：在 ROI 上绘制检出标记与必要辅助线（用于可视化）。

3) 发送数据
- 将检测结果（状态、缺陷列表、统计数据）通过 HTTP 接口上报；
- 触发本地剔废/报警控制（按模式与阈值）；
- 定时发送心跳与产量/剔废统计。

## 6. 常见操作场景

- 设备/相机断电恢复：
  1. 确认硬件通电稳定；
  2. 若程序无响应，先关闭旧进程；
  3. 重新启动 `main.exe`。

- 修改配置（config.yaml / config.json）：
  1. 保存修改；
  2. 关闭 `main.exe`；
  3. 重新启动使配置生效。
