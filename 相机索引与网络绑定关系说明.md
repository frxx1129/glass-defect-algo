# 相机索引与网络绑定关系说明

## 索引类型说明

在GlassAlgo系统中，相机有两种不同类型的索引：

1. **物理索引（Physical Index）**：
   - 由相机SDK在运行时分配
   - 每次启动程序时可能会变化
   - 是相机在运行时的唯一标识
   - 在代码中通常使用`physical_index`表示

2. **逻辑索引（Logical Index）**：
   - 在配置文件中定义，通过MAC地址与相机绑定
   - 用户自定义的相机序号，保持稳定
   - 表示用户对相机的逻辑编号（如相机1、相机2等）
   - 在代码中通常使用`cam_index`或`logical_index`表示

## 相机网络绑定原则

为了确保相机网络分配的稳定性，系统采用以下原则：

1. **基于MAC地址的网卡分配**：
   - 相机的网卡分配使用MAC地址作为唯一标识
   - 当首次设置时，优先使用相机的物理索引匹配网卡索引
   - 设置完成后，这种映射关系将保持稳定

2. **IP分配策略**：
   - IP地址分配基于相机的MAC地址，而非逻辑索引
   - 对于已存在的相机配置，保持原有IP不变（如果在同一网段）
   - 对于新相机，基于MAC地址最后一个字节计算IP地址

3. **配置持久化**：
   - 所有相机与网卡的绑定关系保存在配置文件中
   - 即使逻辑索引变化，也不会影响相机与网卡的物理连接关系

## 代码实现细节

系统中的相机网络分配主要由两个关键函数实现：

1. **`create_camera_nic_map()`**：
   - 创建相机到网卡的映射关系
   - 优先根据物理索引匹配网卡
   - 如果无法匹配，尝试使用逻辑索引或自动分配

2. **`assign_camera_ips()`**：
   - 根据相机与网卡的绑定关系分配IP地址
   - 优先保持现有配置的稳定性
   - 当需要分配新IP时，使用MAC地址确保分配结果的一致性

## 修改说明

最近的代码修改主要解决了以下问题：

1. 移除了对逻辑索引的依赖，避免索引变化导致网络分配混乱
2. 增强了首次设置时网卡分配的可靠性，优先使用物理属性进行绑定
3. 改进了IP分配机制，确保即使调整相机顺序，也不会改变IP分配

这些改进确保了系统在相机序号变化时仍能保持网络连接的稳定性。
